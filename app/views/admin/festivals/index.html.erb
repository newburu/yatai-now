<p style="color: green"><%= notice %></p>

<% content_for :title, "Festivals" %>

<h1>Festivals</h1>

<div id="festivals">
  <% @festivals.each do |festival| %>
    <%= render festival %>
    <p>
      <%= link_to "Show this festival", [:admin, festival] %>
    </p>
  <% end %>
</div>

<%= link_to "New festival", new_admin_festival_path %>
